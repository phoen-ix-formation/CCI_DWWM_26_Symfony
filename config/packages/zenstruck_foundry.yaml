# Config globale — active Foundry dans tous les environnements (dont prod pour le seeding)
zenstruck_foundry:
    persistence:
        flush_once: true

# If you use the `make:factory --test` command, you may need to uncomment the following.
# See https://symfony.com/bundles/ZenstruckFoundryBundle/current/index.html#generate
#when@dev:
#    services:
#        App\Tests\Factory\:
#            resource: '%kernel.project_dir%/tests/Factory/'
#            autowire: true
#            autoconfigure: true

when@test:
    zenstruck_foundry:
        persistence:
            flush_once: true
